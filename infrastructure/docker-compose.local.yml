name: whatsup-dev

services:
  postgres:
    image: "docker.io/library/postgres:17-alpine"
    restart: unless-stopped
    environment:
      - "POSTGRES_DB=keycloak"
      - "POSTGRES_USER=keycloak"
      - "POSTGRES_PASSWORD=keycloak"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keycloak"]
      interval: 5s
      timeout: 5s
      retries: 10

  keycloak:
    image: "quay.io/keycloak/keycloak:26.6.1"
    command: "start-dev --import-realm"
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - "KC_BOOTSTRAP_ADMIN_USERNAME=admin"
      - "KC_BOOTSTRAP_ADMIN_PASSWORD=admin"
      - "KC_DB=postgres"
      - "KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak"
      - "KC_DB_USERNAME=keycloak"
      - "KC_DB_PASSWORD=keycloak"
    volumes:
      - ./keycloak:/opt/keycloak/data/import:ro

  surrealdb:
    image: "docker.io/surrealdb/surrealdb:v2"
    command: "start --bind 0.0.0.0:8000 --user root --pass root file:///data/whatsup.db"
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - surrealdb_data:/data:U

# Backend environment (for cargo run or future containerization)
# These VAPID keys are for local development only — generate new ones for production:
#   npx web-push generate-vapid-keys
x-backend-env: &backend-env
  VAPID_PUBLIC_KEY: "BDxUwwr3NqgybTRRzVQnwS7UbSmRrgaXb6DLc5r8fm5DA7WFvZRg1nPN5h6abqLt6zE4a4Ko_X0Nqykkjy687pU"
  VAPID_PRIVATE_KEY: "cD2ymqynhIRkKPBpG7yNh0fKKH9in0w0xKwKbG-BdQ8"
  VAPID_SUBJECT: "mailto:dev@whatsup.local"

volumes:
  postgres_data:
  surrealdb_data:
