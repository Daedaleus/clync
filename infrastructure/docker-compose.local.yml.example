name: whatsup-dev

services:
  postgres:
    image: "docker.io/library/postgres:17-alpine"
    restart: unless-stopped
    environment:
      - "POSTGRES_DB=keycloak"
      - "POSTGRES_USER=keycloak"
      - "POSTGRES_PASSWORD=keycloak"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keycloak"]
      interval: 5s
      timeout: 5s
      retries: 10

  keycloak:
    image: "quay.io/keycloak/keycloak:26.6.1"
    command: "start-dev --import-realm"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - "KC_BOOTSTRAP_ADMIN_USERNAME=admin"
      - "KC_BOOTSTRAP_ADMIN_PASSWORD=admin"
      - "KC_DB=postgres"
      - "KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak"
      - "KC_DB_USERNAME=keycloak"
      - "KC_DB_PASSWORD=keycloak"
    volumes:
      - ./keycloak:/opt/keycloak/data/import:ro

  surrealdb:
    image: "docker.io/surrealdb/surrealdb:v2"
    command: "start --bind 0.0.0.0:8000 --user root --pass root file:///data/whatsup.db"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - surrealdb_data:/data:U

# Backend environment — VAPID keys for Web Push notifications.
# Generate a fresh key pair: npx web-push generate-vapid-keys
# Copy this file to docker-compose.local.yml and fill in your keys.
x-backend-env: &backend-env
  VAPID_PUBLIC_KEY: ""
  VAPID_PRIVATE_KEY: ""
  VAPID_SUBJECT: "mailto:dev@whatsup.local"

volumes:
  postgres_data:
  surrealdb_data:
